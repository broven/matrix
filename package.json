{
  "name": "matrix",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "sh -c 'set -a; [ -f .env.local ] && . ./.env.local; set +a; exec pnpm -r --parallel dev'"
  }
}
